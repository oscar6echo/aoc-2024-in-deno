// @deno-types="npm:@types/d3"
import { ascending, csvParseRows, descending, max, min, range, sum } from "d3";

export default {
  csvParseRows,
  max,
  min,
  range,
  ascending,
  descending,
  sum,
};
